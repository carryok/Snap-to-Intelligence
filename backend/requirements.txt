fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
httpx>=0.27.0

# Person A's extraction pipeline runs in-process, so its deps are needed here too.
google-generativeai>=0.8.0
pytesseract>=0.3.10
Pillow>=10.0.0
